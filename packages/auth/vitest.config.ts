// See the comment in vitest.package.ts: this is the root config with `include` re-anchored, so
// that running vitest from inside this package finds this package's tests.
export { default } from '../../vitest.package.js'
