{
  "name": "mono",
  "private": true,
  "devDependencies": {
    "@changesets/changelog-github": "^0.2.7",
    "@changesets/cli": "^2.10.3",
    "@types/react": "^16.9.43",
    "@types/react-dom": "^16.9.8",
    "cypress": "^5.2.0",
    "lerna": "^3.15.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "tsdx": "^0.14.0",
    "typescript": "^3.9.7"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna",
    "start": "lerna run start --stream --parallel",
    "start:app": "yarn run build && yarn --cwd demo && yarn --cwd demo start",
    "test": "lerna run test",
    "build": "lerna run build",
    "prepublish": "lerna run prepublish",
    "cypress": "cypress run",
    "cypress:open": "cypress open"
  }
}
